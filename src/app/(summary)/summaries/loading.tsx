import { Skeleton } from "@/components/ui/skeleton";

const Loading = () => {
  return (
    <section>
      <Skeleton />
      <Skeleton />
      <Skeleton />
    </section>
  );
};

export default Loading;
