import { Skeleton } from "@/components/ui/skeleton";

const SummariesDefaultPage = async () => {
  return (
    <section>
      <div>
        <Skeleton />
      </div>
    </section>
  );
};

export default SummariesDefaultPage;
